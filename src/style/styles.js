import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    center: {
        flex:1,
        alignItems : 'center',
        padding: '5%'
    },
    title: {
        fontSize: 36,
        marginBottom: 16,
        marginTop: 50
    }

})